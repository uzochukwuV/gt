{
  "name": "vibe-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "generate-candid": "./scripts/generate-candid.sh",
    "test": "vitest -c tests/vitest.config.ts --root .",
    "preci": "dfx generate && cargo build --target wasm32-unknown-unknown --release --workspace",
    "ci": "vitest run -c tests/vitest.config.ts --root ."
    
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@dfinity/pic": "^0.12.0",
    "@types/node": "^22.14.1",
    "vitest": "^3.1.2"
  }
}
