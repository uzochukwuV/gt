name: End to End

on:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
      - reopened
      - edited
      - synchronize

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04]
        ghc: ["8.8.4"]
        spec:
          - "0.16.1"
        node:
          - 20

    steps:
      - uses: actions/checkout@v3

      - name: Setup Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          target: wasm32-unknown-unknown
          override: true

      - name: Use Node.js ${{ matrix.node }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node }}

      - run: npm install

      - name: Install dfx
        uses: dfinity/setup-dfx@main

      - name: Setup dfx
        run: |
          dfx start --background
          dfx stop

      - run: |
          dfx generate && cargo build --target wasm32-unknown-unknown --release --workspace
          npm run test
        env:
          CI: true
          REPLICA_PORT: 8000
